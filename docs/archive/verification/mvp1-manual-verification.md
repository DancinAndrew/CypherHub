# MVP-1 完整手動驗證清單

> 對應 `develop.md` 196–482 行規格。依序執行可驗收 MVP-1 全功能。
>
> 勾選 `[ ]` 完成後改為 `[x]`。

---

## 一、前置：環境準備

- [ ] **1.1** 啟動 Supabase（local）
  ```bash
  docker compose -f infra/docker-compose.yml up -d
  ```

- [ ] **1.2** 確認 migration 已套用
  ```bash
  supabase db reset
  ```

- [ ] **1.3** 啟動後端
  ```bash
  cd backend && pip install -r requirements.txt && flask run
  ```
  - 預設 `http://localhost:8000`

- [ ] **1.4** 啟動前端
  ```bash
  cd frontend && npm install && npm run dev
  ```
  - 預設 `http://localhost:5173`

- [ ] **1.5** 設定 `.env`
  - `.env`：`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
  - `ADMIN_ALLOWLIST`：Admin 的 `user_id` 或 `email`（逗號分隔）
  - `RESEND_API_KEY`（選填，有則報名成功會寄信）

- [ ] **1.6** Supabase Auth Redirect URLs 含 `http://localhost:5173/reset-password`

---

## 二、MVP-1.0 核心閉環

### 2.1 使用者註冊 / 登入 / 登出（1.0.1）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 開啟 `http://localhost:5173/login` | 看到 Sign In / Sign Up 切換 | [ ] |
| 2 | 點「Need an account? Sign up」 | 切到註冊表單 | [ ] |
| 3 | 輸入 email + 密碼（至少 6 字元）→ Submit | 註冊成功或提示收驗證信 | [ ] |
| 4 | 使用 Sign In 登入 | 成功導向首頁或 `/tickets` | [ ] |
| 5 | 未登入狀態開啟 `http://localhost:5173/tickets` | 導向 `/login?redirect=/tickets` | [ ] |
| 6 | 登入後再開 `/tickets` | 可看到「我的票券」頁（或空列表） | [ ] |
| 7 | 登出（右上或導覽列） | session 清除 | [ ] |
| 8 | 登出後造訪 `/tickets` | 再次導向 `/login?redirect=/tickets` | [ ] |
| 9 | 從 `/login?redirect=/tickets` 登入 | 登入後自動導向 `/tickets` | [ ] |

### 2.2 公開活動列表與詳情（1.0.2）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 未登入開啟 `http://localhost:5173/` | 看到活動列表（僅 published） | [ ] |
| 2 | `curl -s http://localhost:8000/api/v1/events \| jq '.'` | 回傳 `{ "items": [...] }` | [ ] |
| 3 | 點首頁任一活動 | 進入 `/events/:eventId` | [ ] |
| 4 | 活動詳情頁 | 有：時間、地點、票種、描述 | [ ] |
| 5 | 未登入直接開 `http://localhost:5173/events/{某活動ID}` | 可瀏覽，不需登入 | [ ] |

### 2.3 免費報名（1.0.3）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 未登入時，在活動詳情頁點「報名」或選票種 | 導向 `/login?redirect=...` | [ ] |
| 2 | 登入後回到活動頁，選票種 → 送出 | 報名成功 | [ ] |
| 3 | 登入後開 `/tickets` | 看到剛報名的票券 | [ ] |
| 4 | 同一帳號對同一票種超過 `per_user_limit` 報名 | 顯示錯誤（例如「已達每人限購數量」）或 API 400 | [ ] |
| 5 | `curl -X POST .../register` 超過限購 | 回 400，含 `PER_USER_LIMIT_EXCEEDED` | [ ] |

### 2.4 我的票券與 QR（1.0.4）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 登入後開 `/tickets` | 顯示已報名票券列表 | [ ] |
| 2 | 每張票 | 有 QR code 與「Copy Payload」按鈕 | [ ] |
| 3 | 點 Copy Payload | 複製 `{"ticket_id":"...","qr_secret":"..."}` | [ ] |
| 4 | 比較多張票的 `qr_secret` | 每張票不同 | [ ] |

### 2.5 主辦方申請與活動建立（1.0.5）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 登入 → `/organizer/apply` | 看到申請主辦方表單 | [ ] |
| 2 | 填主辦方名稱、簡介 → 送出 | 建立成功，有 `org_id` | [ ] |
| 3 | 前往 `/organizer/events/create` | 看到建立活動表單 | [ ] |
| 4 | 填活動名稱、時間、地點等，status 選「已上架」→ 送出 | 活動建立成功 | [ ] |
| 5 | 在「建立票種」區填名稱、名額、每人限購 → 建立票種 | 票種建立成功 | [ ] |
| 6 | 回首頁 `/` | 可看到剛建立的活動 | [ ] |

### 2.6 QR 核銷（1.0.6）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 主辦方帳號 → `/organizer/checkin` 或 `/organizer/checkin/:eventId` | 看到核銷頁 | [ ] |
| 2 | 選活動（若有下拉），輸入 ticket_id + qr_secret 或貼 JSON payload | 可進行 Verify | [ ] |
| 3 | 點 Verify | `valid=true`, `can_checkin=true` | [ ] |
| 4 | 點 Commit | 第一次 `ok=true`, `already_checked_in=false` | [ ] |
| 5 | 對同一張票再點 Commit | `ok=true`, `already_checked_in=true` | [ ] |
| 6 | 用「非主辦方成員」帳號嘗試 Verify 同活動 | `valid=false`, `reason=FORBIDDEN` | [ ] |

---

## 三、MVP-1.1 活動篩選（1.1）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 首頁點「舞風」篩選（例如 hiphop） | 列表僅顯示該舞風活動 | [ ] |
| 2 | 首頁點「活動類型」篩選（例如 cypher） | 列表僅顯示該類型活動 | [ ] |
| 3 | `curl -s "http://localhost:8000/api/v1/events?styles=hiphop,popping" \| jq '.items | length'` | 回傳符合數量 | [ ] |
| 4 | `curl -s "http://localhost:8000/api/v1/events?types=cypher,battle" \| jq '.'` | 回傳符合類型活動 | [ ] |

---

## 四、MVP-1.2 活動 Metadata 與私密備註（1.2）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 未登入或 Guest 開活動詳情 | 無「主辦方私密備註」 | [ ] |
| 2 | 主辦方開 `/organizer/events/edit`，選自己的活動載入 | 有「主辦方私密備註」欄位可編輯 | [ ] |
| 3 | 活動詳情頁 | 有：報名時間、地圖連結、聯絡信箱/電話、社群連結、流程 (schedule) | [ ] |

---

## 五、MVP-1.3 自訂報名表單（1.3）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 主辦方 → Form Builder（`/organizer/forms`） | 可選活動、建立表單 | [ ] |
| 2 | 建立 event-level 或 ticket-type-level 表單，加欄位（例如文字、勾選） | 表單建立成功 | [ ] |
| 3 | 用戶報名時選有表單的票種 | 顯示動態表單欄位 | [ ] |
| 4 | 填表單後報名成功 | 可完成報名 | [ ] |
| 5 | 主辦方 → Manage → 選活動 → 參加者名單 | 可看到 answers（表單填答） | [ ] |

---

## 六、MVP-1.4 主辦方多頁流程（1.4）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 開 `/organizer` | 主辦方首頁，有：申請主辦方、建立活動、編輯活動、表單設定、管理、核銷 | [ ] |
| 2 | 點「申請成為主辦方」 | 進入 `/organizer/apply` | [ ] |
| 3 | 點「建立活動」 | 進入 `/organizer/events/create` | [ ] |
| 4 | 點「編輯活動」 | 進入 `/organizer/events/edit` | [ ] |
| 5 | 點「表單設定」 | 進入 `/organizer/forms` | [ ] |
| 6 | 點「核銷」 | 進入 `/organizer/checkin` | [ ] |
| 7 | 點「主辦方管理」 | 進入 `/organizer/manage` | [ ] |

---

## 七、MVP-1.5 收尾與穩定化

### 7.1 忘記密碼 / 重設密碼（1.5.1）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 開 `/login`，點「忘記密碼？」 | 切到忘記密碼模式 | [ ] |
| 2 | 輸入已註冊 email → 寄送重設密碼信 | 顯示成功訊息 | [ ] |
| 3 | 收信 → 點連結 | 導向 `/reset-password` | [ ] |
| 4 | 輸入新密碼兩次 → 送出 | 密碼更新，導向首頁 | [ ] |
| 5 | 用新密碼登入 | 可正常登入 | [ ] |

### 7.2 個人資料編輯（1.5.1b）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 登入 → `/profile` | 可編輯 display_name 等欄位 | [ ] |
| 2 | 修改 display_name → 儲存 | 顯示「已儲存」或成功訊息 | [ ] |
| 3 | 重新整理頁面 | display_name 仍為修改後值 | [ ] |

### 7.3 Email 寄送服務串接（1.5.1c）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 若已設定 `RESEND_API_KEY` | 報名成功後可收信 | [ ] |
| 2 | 報名成功後檢查信箱 | 信件含活動名稱、票券/「我的票券」相關連結 | [ ] |
| 3 | 未設定 Resend | 報名仍成功，後端 log 有 stub 訊息 | [ ] |

### 7.4 活動圖片管理（1.5.2a）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 主辦方編輯活動，在「活動圖片」區上傳圖片 | 上傳成功 | [ ] |
| 2 | 開活動詳情 | 有圖時顯示輪播 | [ ] |
| 3 | 多張圖 | 可左右切換、顯示 1/N | [ ] |

### 7.5 主辦方資訊與其他活動（1.5.2b）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 開活動詳情 | 有主辦方區塊（名稱、簡介、聯絡） | [ ] |
| 2 | 該主辦方有其他 published 活動 | 顯示「同主辦方其他活動」列表 | [ ] |
| 3 | 點其他活動 | 可進入該活動詳情 | [ ] |

### 7.6 主辦方代參加者重寄票券（1.5.2c）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 主辦方 → Manage → 選活動 | 看到參加者名單 | [ ] |
| 2 | 對某張票點「重寄票券」 | 顯示「已重寄票券信至參加者信箱」 | [ ] |
| 3 | 參加者檢查信箱 | 收到重寄的票券信（若 Resend 已設定） | [ ] |

### 7.7 核銷統計 Dashboard（1.5.2d）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 主辦方 → Manage → 選活動 | 顯示「已入場 N / 未入場 M」 | [ ] |
| 2 | 同一區塊 | 有按票種的統計表格（已入場 / 未入場） | [ ] |
| 3 | 主辦方 → Checkin 核銷頁 | 也有相同統計顯示 | [ ] |

### 7.8 進階搜尋與篩選（1.5.2e）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 首頁 | 有搜尋框（關鍵字） | [ ] |
| 2 | 輸入關鍵字 → 套用篩選 | 列表依關鍵字變化 | [ ] |
| 3 | 首頁 | 有「開始日期」「結束日期」篩選 | [ ] |
| 4 | 選日期範圍 → 套用 | 列表依日期區間變化 | [ ] |
| 5 | `curl -s "http://localhost:8000/api/v1/events?q=關鍵字" \| jq '.'` | 回傳符合活動 | [ ] |
| 6 | `curl -s "http://localhost:8000/api/v1/events?from=2025-01-01&to=2025-12-31" \| jq '.'` | 回傳日期區間內活動 | [ ] |

### 7.9 活動分享連結（1.5.2f） ✅

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 開活動詳情 | 有「分享活動」按鈕 | [ ] |
| 2 | 點分享 | 複製當前活動 URL 到剪貼簿 | [ ] |
| 3 | 顯示「已複製連結」等提示 | 有成功回饋 | [ ] |
| 4 | 貼 URL 到新分頁 | 可開啟同活動詳情 | [ ] |

### 7.10 活動編輯限制（1.5.2g）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 主辦方編輯「已上架」活動 | 有黃色警告（敏感欄位說明） | [ ] |
| 2 | 修改活動開始/結束時間 → 儲存 | 跳出確認對話 | [ ] |
| 3 | 在既有票種編輯中，將 capacity 改為小於已售數量 | 前端或 API 阻擋，顯示「名額不可小於已售出數量」 | [ ] |
| 4 | 對 `sold_count > 0` 的票種點「刪除」 | 按鈕 disabled 或顯示「已售出不可刪除」 | [ ] |
| 5 | 對 `sold_count = 0` 的票種點「刪除」 | 可確認後刪除 | [ ] |
| 6 | 票種編輯（名稱、名額、每人限購、開賣時間等）→ 儲存 | 更新成功 | [ ] |

### 7.11 活動狀態機完整流轉（1.5.3a）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 建立活動時選 status=草稿 | 建立成功，首頁看不到 | [ ] |
| 2 | 編輯活動，改為「已上架」 | 首頁可見 | [ ] |
| 3 | 編輯活動，改為「已結束」 | 活動不再公開，不可報名 | [ ] |
| 4 | 編輯活動，改為「已取消」 | 同上 | [ ] |
| 5 | 對 ended/cancelled 活動的票嘗試核銷 | Verify 回 `valid=false` 或 `can_checkin=false` | [ ] |
| 6 | GET /events | 僅回 status=published 活動 | [ ] |

### 7.12 後台前端介面（1.5.3b）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 使用 `ADMIN_ALLOWLIST` 中的帳號登入 | 可造訪 | [ ] |
| 2 | 開 `/admin` | 可進入 Admin 頁 | [ ] |
| 3 | Admin 頁 | 顯示全站活動列表（含 draft、published、ended、cancelled、disabled） | [ ] |
| 4 | 非 allowlist 帳號開 `/admin` | 403 或導向無權限頁 | [ ] |

### 7.13 下架活動 API（1.5.3c）

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | Admin 對 published 活動點「下架」 | 更新成功 | [ ] |
| 2 | 回首頁 | 該活動不再出現在列表 | [ ] |
| 3 | `curl -s http://localhost:8000/api/v1/events \| jq '.items[] | select(.id=="...")'` | 無該活動 | [ ] |

### 7.14 Rate Limiting（1.5.3d） ✅

| 序 | 操作 | 預期結果 | 勾選 |
|---|------|----------|------|
| 1 | 短時間內連續登入失敗超過 10 次（同 IP） | 回 429，「操作過於頻繁，請稍後再試」 | [ ] |
| 2 | 短時間內對同一活動報名超過 20 次 | 回 429 | [ ] |
| 3 | 短時間內核銷 Verify/Commit 超過 60 次 | 回 429 | [ ] |

---

## 八、快速 API 驗證指令

```bash
# 健康檢查
curl -s http://localhost:8000/api/v1/health | jq

# 公開活動列表
curl -s http://localhost:8000/api/v1/events | jq '.items | length'

# 關鍵字搜尋
curl -s "http://localhost:8000/api/v1/events?q=test" | jq

# 舞風篩選
curl -s "http://localhost:8000/api/v1/events?styles=hiphop" | jq '.items | length'

# 日期篩選
curl -s "http://localhost:8000/api/v1/events?from=2025-01-01&to=2025-12-31" | jq '.items | length'
```

---

## 九、驗證完成簽核

| 項目 | 日期 | 執行者 | 備註 |
|------|------|--------|------|
| MVP-1.0 核心閉環 | | | |
| MVP-1.1 活動篩選 | | | |
| MVP-1.2 Metadata | | | |
| MVP-1.3 報名表單 | | | |
| MVP-1.4 主辦方流程 | | | |
| MVP-1.5 收尾與穩定化 | | | |
